# Em desenvolvimento, o Vite proxy já redireciona /api para o backend.
# Em produção, o nginx cuida do proxy.
# Só use esta variável se o backend estiver em outro domínio:
# VITE_API_URL=https://api.exemplo.com
